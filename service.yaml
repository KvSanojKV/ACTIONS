apiVersion: v1
kind: Service
metadata:
  name: app-vmware-service
spec:
  type: NodePort
  selector:
    app: app-vmware
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 31014