name: My Test workflow
on: push
jobs:
    build_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repo
              uses: actions/checkout@v7
            - name: build docker image
              run: docker build -t .