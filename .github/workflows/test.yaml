name: test pipeline
on: push
jobs:
  build-job:
    runs-on: ubuntu-latest
    steps:
    - name: Check uptime of runner
      run: uptime
    - name: create a file
      run: echo "test" > text.txt
    - name: read file
      run: cat text.txt