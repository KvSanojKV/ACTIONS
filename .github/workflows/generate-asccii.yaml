name: Generate ASCII
on: push


jobs:
   ascii_job:
     runs-on: ubuntu-latest
     steps:
       - name: Checkout repo
         uses: actions/checkout@v7
       - name: list files
         run: ls -ltraR
       - name: Executing shell script.
         run: sh ./script.sh
         
    #   - name: list files
    #     run: ls 
    #   - name: Read files
    #     run: cat README.md
       