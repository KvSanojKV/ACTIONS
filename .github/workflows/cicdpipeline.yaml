name: Test pipeline working
on: push
jobs:
    build:
        runs-on: ubuntu-latest
        steps:

        - name: Create a file
          run: touch test.txt
        - name: list file
          run: ls -ltr